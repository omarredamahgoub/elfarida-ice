# دليل التشغيل والصيانة — موقع «الفريدة آيس»

> **الغرض:** هذا الملفّ هو المرجع الموثوق الوحيد لتشغيل الموقع وصيانته ونشره. يُحدَّث عند كلّ تغيير جوهريّ في البنية أو الإعداد.
> **آخر تحديث:** 16 يونيو 2026 · **النطاق:** `elfaridaice.com` · **مستودع الكود:** `github.com/omarredamahgoub/elfarida-ice`
> **ملاحظة سرّيّة:** لا يحوي هذا الملفّ أيّ قيمة سرّيّة (مفاتيح أو كلمات مرور)؛ تُخزَّن جميعها في قاعدة البيانات كما هو موضَّح أدناه. هذا الملفّ مستثنًى من النشر العلنيّ عبر `.wranglerignore`.

---

## 1) نظرة معماريّة عامّة

الموقع تطبيقٌ هجين على **Cloudflare Pages**:

أولًا، **طبقة الأصول الثابتة:** صفحات HTML ثنائيّة اللغة (عربيّ/إنجليزيّ) وملفّات CSS وJavaScript وصور WebP، تُخدَّم مباشرةً من حافة Cloudflare.

ثانيًا، **طبقة الدوالّ (Pages Functions):** كود تنفيذيّ على الحافة داخل مجلّد `functions/`، يوفّر نقطتَي خدمة ديناميكيّتين: استقبال طلبات عروض الأسعار، ولوحة إدارة محميّة لعرضها.

ثالثًا، **طبقة التخزين (Cloudflare D1):** قاعدة بيانات SQLite مُدارة باسم `elfarida-leads`، مرتبطة بالمشروع عبر الرباط `DB`، تخزّن الطلبات الواردة وإعدادات التشغيل الحسّاسة.

رابعًا، **طبقة الإشعار (Resend):** خدمة بريد عبر واجهة برمجيّة (API) تُرسِل إشعارًا بكلّ طلب جديد إلى البريد الإلكترونيّ المحدَّد.

التدفّق الكامل لأيّ طلب عرض سعر:

```
نموذج الموقع  ←  POST /api/quote  ←  حفظ في D1 (المصدر الدائم)  ←  إشعار عبر Resend  ←  بريد إلى المستقبِلين
```

---

## 2) منظومة طلبات عروض الأسعار (المسار الكامل)

**الدالّة:** `functions/api/quote.js` — تستقبل طلبات `POST /api/quote`.

مراحل المعالجة، وكلٌّ منها يتدهور بأمان إن غاب شرطه:

1. **تحليل الجسم:** يقبل JSON أو نموذجًا مُرمَّزًا (form-encoded).
2. **مصيدة السبام (honeypot):** الحقل `botcheck` — إن امتلأ، يُردّ بنجاح صوريّ ويُتجاهَل الطلب (لإرباك الروبوتات).
3. **تحقّق Turnstile:** يُفرَض فقط إن وُجد المتغيّر `TURNSTILE_SECRET` (غير مفعَّل حاليًّا — اختياريّ).
4. **تحقّق أدنى:** وجود اسم أو هاتف أو بريد، وصحّة صيغة البريد.
5. **الحفظ في D1:** إدراج صفّ في جدول `leads` — وهو **المصدر الدائم الموثوق** للطلبات.
6. **الإشعار البريديّ:** إرسال عبر Resend إن توفّر المفتاح والمستقبِلون في الإعداد.

> **مبدأ تصميميّ مهمّ:** الطلب يُعَدّ ناجحًا إذا **حُفِظ في D1 أو أُرسِل بالبريد**. هذا يضمن ألّا يضيع أيّ طلب حتّى لو تعطّل البريد مؤقّتًا؛ فالنسخة الدائمة تبقى في قاعدة البيانات ولوحة الإدارة.

**النماذج المرتبطة:** جميع نماذج الموقع (صفحة التواصل العربيّة والإنجليزيّة، والتذييل) تُرسِل إلى `/api/quote` بالصيغة نفسها التي كانت تُرسَل بها سابقًا إلى الخدمة الخارجيّة.

---

## 3) لوحة الإدارة (عرض الطلبات)

**الدالّة:** `functions/admin/leads.js` — متاحة على `https://elfaridaice.com/admin/leads`.

الخصائص:

- **مصادقة عبر جلسة موقَّعة (HMAC cookie):** اسم الكوكي `efi_admin`، صلاحيّة ٨ ساعات.
- **الإعداد عند أوّل زيارة (TOFU):** إن لم تكن بيانات الدخول مضبوطة، تطلب اللوحة تعيينها أوّل مرّة.
- **بيانات الدخول مخزَّنة في D1** (جدول `settings`): اسم المستخدم `admin_user`، وبصمة كلمة المرور `admin_pwd_hash` (SHA-256). لا تُخزَّن كلمة المرور نصًّا.
- **التصدير:** إمكانيّة تنزيل الطلبات بصيغتَي CSV وJSON.
- **تسجيل الخروج** يُنهي الجلسة.

> **لماذا في D1 لا في متغيّرات البيئة؟** ثبت عمليًّا على هذا المشروع أنّ أسرار البيئة (Environment Secrets) لا تصل إلى الدوالّ بشكل موثوق، بينما رباط قاعدة D1 يصل دائمًا. لذلك اعتُمدت D1 مصدرًا وحيدًا لكلّ الإعدادات الحسّاسة (بيانات اللوحة، ومفتاح البريد، والمُرسِل، والمستقبِلين).

---

## 4) قاعدة البيانات D1 والإعدادات

**اسم القاعدة:** `elfarida-leads` · **الرباط في المشروع:** `DB` · **المعرّف (UUID):** `0637575c-303e-4150-9810-e96ea462ad49`

### 4.1 جدول `leads` (الطلبات الواردة)
الأعمدة: `id`, `created_at`, `name`, `email`, `phone`, `subject`, `payload` (JSON كامل), `ip`, `ua`. مُخطَّط الإنشاء في `migrations/0001_init.sql`.

### 4.2 جدول `settings` (الإعداد الحسّاس — مفتاح/قيمة)

| المفتاح | الغرض |
|---|---|
| `admin_user` | اسم مستخدم لوحة الإدارة |
| `admin_pwd_hash` | بصمة كلمة مرور اللوحة (SHA-256، لا نصّ صريح) |
| `resend_api_key` | مفتاح Resend لإرسال البريد |
| `lead_from` | عنوان واسم المُرسِل: `الفريدة آيس <no-reply@elfaridaice.com>` |
| `lead_to` | المستقبِلون (مفصولون بفاصلة) — حاليًّا: `omahgoob321@gmail.com,elfaridaice@gmail.com` |
| `turnstile_secret` | مفتاح Turnstile السرّيّ — حذفه يُعطّل التحقّق فورًا (مفيد للطوارئ) |

### 4.3 أوامر إدارة شائعة (تُنفَّذ عبر wrangler)

عرض كلّ الإعدادات (مع إخفاء القيم السرّيّة):
```powershell
npx wrangler d1 execute elfarida-leads --remote --command "SELECT k FROM settings;"
```

تغيير مستقبِلي الإشعارات:
```powershell
npx wrangler d1 execute elfarida-leads --remote --command "UPDATE settings SET v='a@x.com,b@y.com' WHERE k='lead_to';"
```

تدوير مفتاح Resend (بعد إنشاء مفتاح جديد في لوحة Resend):
```powershell
npx wrangler d1 execute elfarida-leads --remote --command "UPDATE settings SET v='re_NEW_KEY' WHERE k='resend_api_key';"
```

عرض آخر الطلبات:
```powershell
npx wrangler d1 execute elfarida-leads --remote --command "SELECT created_at,name,phone,subject FROM leads ORDER BY created_at DESC LIMIT 20;"
```

> القيم السرّيّة (المفتاح وكلمة المرور) تُدخَل عبر هذه الأوامر من طرفيّتك مباشرةً، ولا تُكتب في أيّ ملفّ داخل المستودع.

---

## 5) إجراء النشر (مهمّ جدًّا)

للموقع مساران للنشر، كلاهما يبني المشروع نفسه من المصدر ذاته:

### 5.1 المسار الموصى به — الدفع إلى GitHub
أيّ دفعٍ إلى الفرع `main` يُشغّل بناءً آليًّا في Cloudflare يضمّن مجلّد `functions/` تلقائيًّا:
```powershell
cd D:\elfarida-ice-deploy
git add -A
git commit -m "وصف التغيير"
git push origin main
```

### 5.2 النشر اليدويّ المباشر — مع تحذير حرج
بوجود `wrangler.toml` في جذر المشروع (يحدّد اسم المشروع ومجلّد الإخراج ورباط D1)، صار أمر النشر مبسَّطًا — يُنفَّذ **من داخل مجلّد المشروع** بلا وسائط:
```powershell
cd D:\elfarida-ice-deploy
npx wrangler pages deploy
```

> ⚠️ **تحذير حرج (سبب عطل سابق):** يجب تنفيذ أمر النشر **من داخل مجلّد المشروع** `D:\elfarida-ice-deploy` مع المسار `.` (نقطة). إن نُفِّذ من مجلّد آخر مع تمرير المسار وسيطًا، فإنّ أداة wrangler **لا تجد مجلّد `functions/` ولا تُضمّنه في النشر**، فتتوقّف نقاط `/api/quote` و`/admin/leads` عن العمل وتردّ بالخطأ 405 — دون أيّ رسالة خطأ واضحة.
>
> **علامة النجاح:** يجب أن يظهر في مخرجات النشر السطران: `✨ Compiled Worker successfully` و`✨ Uploading Functions bundle`. غيابهما يعني أنّ الدوالّ **لم تُنشَر**.

### 5.3 التحقّق بعد كلّ نشر
```powershell
# يجب أن يردّ هذا بـ success:true لا بخطأ 405
# (يُجرى من سياق الموقع في المتصفّح، أو راجع لوحة Resend وقاعدة D1)
```
تأكّد أنّ `POST /api/quote` يردّ `{"success":true}`، وأنّ صفحة `/admin/leads` تفتح.

---

## 6) التحقّق من عمل البريد فعليًّا

أرسِل طلبًا تجريبيًّا عبر نموذج التواصل، ثمّ:

1. **في لوحة Resend** (`resend.com/emails`، مساحة العمل التي تحوي نطاق `elfaridaice.com`): يجب أن يظهر البريد بحالة **Delivered** وبعلامة **+1** (دلالة الإرسال للعنوانين معًا).
2. **في قاعدة D1:** يجب أن يظهر الطلب في جدول `leads`.
3. **نظِّف صفوف الاختبار** بعد التأكّد:
```powershell
npx wrangler d1 execute elfarida-leads --remote --command "DELETE FROM leads WHERE name LIKE '%اختبار%' OR name LIKE '%probe%';"
```

> **توثيق نطاق المُرسِل في Resend:** نطاق `elfaridaice.com` موثَّق عبر سجلّ DKIM في Cloudflare DNS (لا يمسّ سجلّات البريد الواردة على Namecheap). إن أُعيد إنشاء الحساب أو تغيّر النطاق، يلزم إعادة التوثيق.

---

## 7) الواجهة الأماميّة: نقاط تشغيليّة

**مبدّل اللغة** (`js/lang-switch.js`): يحوّل بين النسخة العربيّة والإنجليزيّة بضغطة واحدة. عُولِجت سابقًا مشكلة «الضغطتين» الناتجة عن الروابط النظيفة (clean URLs) بإضافة تطبيع امتداد `.html` عند استخراج اسم الصفحة.

**الخطّ الافتراضيّ:** الخطّ الموحَّد لكامل الموقع هو **Cairo**، مُعرَّف في `cairo.css` بثلاث كتل `@font-face` (عربيّ، لاتينيّ ممتدّ، لاتينيّ) بأوزان 300–900 و`font-display: swap`، ومربوط مطلقًا في كلّ الصفحات: `/cairo.css?v=...`.

**الأيقونات:** تُحمَّل Font Awesome من شبكة cdnjs على جميع الصفحات (لا تُستضاف محلّيًّا؛ مجلّد `/webfonts/` غير موجود في المستودع). أيّ صفحة جديدة يجب أن تستخدم رابط cdnjs نفسه لتظهر الأيقونات.

**هويّة الشعار:** ارتفاع الترويسة وأبعاد الشعار مضبوطة في CSS الحرج المضمَّن في كلّ صفحة لتثبيت الهويّة البصريّة ومنع «تدلّي» الشعار في الهيرو.

**إبطال ذاكرة التخزين (Cache busting):** الأصول الثابتة (JS/CSS) تُخدَّم بتخزين طويل غير قابل للتغيّر؛ لذلك **عند أيّ تعديل على ملفّ أصول، يجب رفع بصمة الإصدار** في مرجعه داخل HTML (`?v=YYYYMMDD`) حتّى يصل التحديث للزوّار العائدين.

**اسم الشركة:** الصيغة المعتمدة في كامل الموقع هي **«شركة الفريدة»** (لا «مؤسسة الفريدة»).

---

## 8) الأمان (الوضع الراهن)

- **الترويسات** (`_headers`): `HSTS` مع `preload`، و`X-Content-Type-Options: nosniff`، و`frame-ancestors 'none'`، و`Referrer-Policy`، و`Permissions-Policy`، و`Cross-Origin-Opener-Policy`، وسياسة محتوى (CSP) فعليّة. أُزيلت الترويسة المهجورة `X-XSS-Protection` و`'unsafe-hashes'`.
- **لا أسرار في الكود:** كلّ القيم الحسّاسة في D1، والملفّات الوصفيّة والتطويريّة (`*.md`, `*.ps1`) مستثناة من النشر عبر `.wranglerignore`.
- **CSP (مُصلَّبة):** لم تَعُد تسمح بـ`'unsafe-inline'`. تُبنى ديناميكيًّا في `functions/_middleware.js` بـ**nonce لكلّ طلب** يُحقَن في كلّ `<script>`/`<style>` مضمَّن؛ وأُزيلت كلّ معالجات الأحداث المضمّنة (٢٥٢ معالجًا) إلى مستمعات خارجيّة/مرتبطة. لا تَضع CSP في `_headers` بعد الآن — الـ middleware هو مصدرها الوحيد.
- **النماذج:** محميّة بمصيدة honeypot، مع إمكانيّة تفعيل Turnstile لاحقًا.
- **سياسة الإفصاح الأمنيّ:** `/.well-known/security.txt` وفق RFC 9116.

---

## 9) خريطة الملفّات الأساسيّة

```
functions/api/quote.js      استقبال الطلبات → D1 → بريد Resend
functions/admin/leads.js    لوحة الإدارة المحميّة (عرض/تصدير)
functions/_middleware.js     حقن nonce وبناء سياسة CSP لكلّ طلب
migrations/0001_init.sql     مُخطَّط جدول leads
scripts/backup-leads.ps1     نسخ احتياطيّ أسبوعيّ لجدول leads (مجدوَل)
wrangler.toml                إعداد المشروع: رباط D1 + مجلّد الإخراج
_headers                     ترويسات الأمان والتخزين
_redirects                   إعادة توجيه روابط ووردبريس القديمة + التطبيع
worker.js                    خدمة الأصول وصفحة 404 (أصل ثابت)
cairo.css                    تعريف خطّ Cairo الموحَّد
js/lang-switch.js            مبدّل اللغة
js/site-shell.js             الطبقة المشتركة + حقن المحتوى (مع تهريب HTML)
robots.txt / sitemap*.xml    ضبط الزواحف وخرائط الموقع
.well-known/security.txt     سياسة الإفصاح الأمنيّ
.wranglerignore              استبعاد ملفّات التطوير من النشر
```

---

## 10) سجلّ الأعمال المؤجّلة (Backlog)

1. ✅ **(عولِج) ترتيب `_redirects`:** نُقِلت القاعدة الشاملة `/ar/*` إلى نهاية الملفّ، وأُضيفت قواعد بالصيغة المفكوكة (Arabic literal) للصفحات الستّ القديمة. ملاحظة: Cloudflare يطابق المسار **بعد فكّ الترميز**، لذا تعمل القواعد المفكوكة مع التصفّح الطبيعيّ، ويبقى ما لا يُطابَق متجهًا إلى `/blog.html` كحلٍّ آمن.
2. ✅ **(عولِج) تصليب CSP:** أُزيل `'unsafe-inline'` بالكامل عبر nonce لكلّ طلب في `functions/_middleware.js`، ونُقِلت ٢٥٢ معالج حدث مضمّنًا (٢٤٢ منها نمط preload للأنماط) إلى مستمعات خارجيّة. تأكّد حيًّا: لا انتهاكات CSP، والأنماط والسكربتات والفلاتر تعمل.
3. ⛔ **(دُرِس وتُقرِّر الإبقاء) حصر `img-src`:** الموقع يستخدم Meta Pixel وGoogle Analytics اللذين يطلقان صور تتبّع من نطاقات خارجيّة؛ لذا تضييق `img-src` يكسر التتبّع. الإبقاء على `https:` هنا وضعٌ مقصود وسليم.
4. ✅ **(عولِج) تفعيل Turnstile:** أُضيفت ودجة Turnstile (وضع Managed) إلى نماذج طلب عرض السعر الأربعة (`contact`, `contact-en`, `index`, `index-en`) مع علامة `protected=1`. الدالّة تتحقّق من الرمز **فقط** للنماذج التي تحمل العلامة (فلا تتأثّر نشرة التذييل). المفتاح السرّيّ في D1 (`turnstile_secret`)، والسماح بـ`challenges.cloudflare.com` مُضاف إلى CSP. تأكّد حيًّا: الودجة تظهر للبشر، والإرسال المحميّ بلا رمز يُرفض (403).
5. ✅ **(عولِج) حفظ إعداد النشر:** أُضيف `wrangler.toml` في جذر المستودع موثِّقًا رباط D1 ومجلّد الإخراج، فصارت البنية قابلة لإعادة الإنتاج.
6. ✅ **(عولِج جزئيًّا) نسخ احتياطيّ دوريّ لجدول `leads`:** سكربت `scripts/backup-leads.ps1` يصدّر جدول الطلبات (دون جدول الأسرار) إلى `backups/leads-<تاريخ>.json` ويحتفظ بآخر ٢٦ نسخة، ومجدوَل أسبوعيًّا (الإثنين) عبر مهمّة `elfarida-leads-backup`. ✅ أُضيفت **رسالة تأكيد تلقائيّة للعميل** (بريد مُنسَّق بالعربيّة يُرسَل لمن أدخل بريدًا صالحًا، أفضل جهد، عبر `sendCustomerConfirmation`). ✅ كما استُبدِل مفتاح Resend بآخر بصلاحيّة **الإرسال فقط** مقيَّد بنطاق `elfaridaice.com` (مبدأ الامتياز الأدنى) — مخزَّن في D1، وتأكّد عمله حيًّا.
7. ✅ **(عولِج) حماية النموذج من السبام:** أُضيف حدّ معدّل لكلّ IP عبر D1 (٥ طلبات/١٠ دقائق) وحدّ لحجم الطلب (٨ ك.ب) في `functions/api/quote.js`، إضافةً إلى مصيدة honeypot. يبقى Turnstile (البند ٤) كطبقة أقوى عند توفّر المفاتيح.

---

## 11) جهات الاتّصال والبيانات الثابتة

- **الشركة:** شركة الفريدة آيس للهندسة والتبريد الصناعيّ — الدمّام، المنطقة الشرقيّة، السعوديّة.
- **البريد:** `info@elfaridaice.com` · **الهاتف:** ‎+966 59 836 6214‎.
- **مستقبِلو إشعارات الطلبات:** `omahgoob321@gmail.com` و`elfaridaice@gmail.com` (قابلان للتغيير عبر `lead_to` في D1).
