# تقرير فحص فنّيّ وأمنيّ — موقع «الفريدة آيس»

**المسار المفحوص:** `D:\elfarida-ice-deploy`
**نوع المشروع:** موقع ثابت ثنائيّ اللغة (عربي/إنجليزي) منشور على Cloudflare Workers (أصول ثابتة) — `elfaridaice.com`
**تاريخ الفحص:** 10 يونيو 2026
**طبيعة التقرير:** مراجعة حياديّة، تشمل نقاط القوّة والملاحظات معًا، مع مستويات خطورة وتوصيات عمليّة.

> ⚠️ **هذا التقرير لقطةٌ بتاريخ 10 يونيو 2026.** عُولِجت أغلب ملاحظاته منذ ذلك الحين، وتغيّرت بنية النماذج جوهريًّا. للوضع الراهن والتشغيل اليوميّ، المرجع المعتمد هو **«دليل التشغيل والصيانة»** (`دليل-التشغيل-والصيانة.md`). انظر «تحديث الحالة» أدناه.

---

## 0) تحديث الحالة (16 يونيو 2026)

| الملاحظة | الحالة الراهنة |
|---|---|
| **L-2** خدمة Web3Forms الخارجيّة | ✅ **أُزيلت بالكامل** واستُبدِلت بمنظومة ذاتيّة على Cloudflare: `POST /api/quote` ← قاعدة D1 ← إشعار بريديّ عبر Resend، مع لوحة إدارة محميّة `/admin/leads`. |
| **L-3** ترويسة `X-XSS-Protection` | ✅ أُزيلت. |
| **L-1** `'unsafe-hashes'` و`'unsafe-inline'` في CSP | ✅ **عولِجا بالكامل** — أُزيل `'unsafe-inline'` عبر nonce لكلّ طلب في middleware، ونُقِلت ٢٥٢ معالج حدث مضمّنًا. |
| **M-2** إبطال ذاكرة التخزين | ✅ عولِج باعتماد بصمات إصدار `?v=` على مراجع الأصول. |
| **M-1** ترتيب قواعد `_redirects` | ✅ **عولِج** — نُقِلت `/ar/*` إلى النهاية وأُضيفت قواعد بالصيغة المفكوكة للصفحات الستّ، وتأكّد عملها حيًّا. |
| **I-1/I-2** تنظيف `package.json`/`.gitignore` | ✅ عولِج. |
| **I-3** غياب `wrangler.toml` | ✅ **عولِج** — أُضيف `wrangler.toml` موثِّقًا رباط D1 ومجلّد الإخراج. |

> ملاحظة: القسمان أدناه (L-2 خاصّةً) يصفان الوضع **قبل** المعالجة، ويُبقَيان للتوثيق التاريخيّ فقط.

---

## 1) الملخّص التنفيذيّ

الموقع **مبنيّ بمستوى هندسيّ وأمنيّ مرتفع** مقارنةً بالمواقع المماثلة. لم يُرصَد أيّ تسريب لأسرار حقيقيّة، ولا ثغرة حقن نصوص (XSS) قابلة للاستغلال، والترويسات الأمنيّة قويّة، وطبقة العرض الديناميكيّة تُهرّب مخرجاتها بشكل سليم.

الملاحظات المرصودة محدودة، وأغلبها يتعلّق بالصحّة الوظيفيّة وتحسين الظهور والتشغيل، لا بثغرات أمنيّة خطيرة.

| الخطورة | العدد |
|---|---|
| حرجة | 0 |
| عالية | 0 |
| متوسّطة | 2 |
| منخفضة | 4 |
| معلوماتيّة | 3 |

**أبرز ما يجب إصلاحه أوّلًا:** تعارض ترتيب قواعد إعادة التوجيه (يُفقِد روابط قديمة قيمتها)، وفجوة إبطال ذاكرة التخزين المؤقّت للأصول (تمنع وصول التحديثات للزوّار العائدين).

---

## 2) نطاق الفحص والمنهجيّة

شمل الفحص: ملفّ الـWorker، وقواعد الترويسات `_headers`، وإعادة التوجيه `_redirects`، والطبقة المشتركة `site-shell.js`، ومعالجات النماذج (`contact.js`)، وملفّات الحقن الديناميكيّ، وإعدادات الحزمة و`.gitignore`، إضافةً إلى عمليّات بحث نمطيّة عبر المشروع عن: الأسرار ومفاتيح الواجهات، وأنماط الحقن الخطرة (`innerHTML`/`eval`/`document.write`)، وقراءة معطيات الرابط، والمحتوى غير الآمن (`http://`)، وروابط `_blank`، ووسوم `hreflang`.

---

## 3) نقاط القوّة (إيجابيّات مؤكَّدة)

- **ترويسات أمان قويّة:** `Strict-Transport-Security` مع `preload`، و`X-Content-Type-Options: nosniff`، و`frame-ancestors 'none'`، و`Referrer-Policy`، و`Permissions-Policy`، و`Cross-Origin-Opener-Policy: same-origin`، ووجود سياسة أمان محتوى (CSP) فعليّة.
- **تهريب مخرجات سليم:** `site-shell.js` يطبّق دالّة تهريب HTML على كل القيم الديناميكيّة قبل إدراجها في DOM — ممارسة تمنع XSS من البيانات.
- **لا قراءة لمعطيات الرابط:** لا استخدام لـ`location.search`/`location.hash`/`URLSearchParams`، أي لا يوجد مسار حقن منعكس (Reflected XSS).
- **لا أسرار حقيقيّة مكشوفة:** المفتاح الظاهر (`770521ce-...`) هو مفتاح Web3Forms العلنيّ بالتصميم، لا مفتاح خادم سرّيّ.
- **روابط خارجيّة آمنة:** كل روابط `target="_blank"` تحمل `rel="noopener noreferrer"` (لا Tabnabbing).
- **لا محتوى مختلط:** لا وجود لأيّ `src`/`href` عبر `http://` (كل الموارد آمنة/نسبيّة).
- **حماية من السبام:** حقل مصيدة (honeypot) في النماذج + تحقّق من المدخلات على العميل.
- **SEO ثنائي اللغة سليم:** وسوم `hreflang` (ar/en/x-default) **ثابتة في رأس الصفحات**، مع canonical و`robots` وخرائط موقع وملفّ `manifest` (PWA) وCSS حرج مضمّن وpreconnect.
- **Worker نظيف:** يخدم الأصول، ويطبّع الجذر إلى `index.html`، ويقدّم صفحة 404 مخصّصة، دون معالجة مدخلات أو أسرار.

---

## 4) النتائج حسب الخطورة

### 🟠 متوسّطة

#### M-1 — تعارض ترتيب قواعد إعادة التوجيه يُبطِل قواعد محدّدة
**الملف:** `_redirects`
**الوصف:** القاعدة الشاملة `‎/ar/*  →  /blog.html‎` (القسم 4) مكتوبة **قبل** القواعد المحدّدة لروابط ووردبريس العربيّة المُرمَّزة (الأقسام 10–12) وقبل `‎/ar/industries/*‎` (القسم 9). وبما أنّ Cloudflare يطبّق **أوّل قاعدة مطابقة من الأعلى للأسفل**، فإنّ كل القواعد المحدّدة تحت `/ar/` **لن تُنفَّذ أبدًا**؛ فمثلًا رابط «من نحن» القديم `‎/ar/%d9%85%d9%86-%d9%86%d8%ad%d9%86/‎` سيتحوّل إلى `blog.html` بدل `about.html`.
**الأثر:** فقدان قيمة الروابط القديمة (link equity) وتوجيه الزائر/محرّك البحث لصفحة خاطئة — ضرر مباشر على SEO وتجربة المستخدم.
**التوصية:** انقل كل القواعد المحدّدة لـ`/ar/...` و`/ar/industries/*` إلى **أعلى** القاعدة الشاملة `‎/ar/*  →  /blog.html‎`، بحيث تأتي العامّة في الأخير.

#### M-2 — فجوة إبطال ذاكرة التخزين للأصول (التحديثات لا تصل للزوّار العائدين)
**الملف:** `_headers`
**الوصف:** ملفّات `‎/*.js‎` و`‎/*.css‎` تُخدَّم بـ`Cache-Control: public, max-age=31536000, immutable` بأسماء **ثابتة** (مثل `index.js`، `shared.css`). صفحات HTML مضبوطة على `no-cache` (تُعاد مراجعتها)، لكنّها تشير إلى نفس أسماء ملفّات الأصول، فلا يُعيد المتصفّح جلبها عند تحديث محتواها — قد يبقى الزائر العائد على نسخة قديمة حتى سنة.
**الأثر:** أيّ تعديل على JS/CSS قد لا يظهر للزوّار العائدين، ما قد يسبّب سلوكًا غير متّسق أو أعطالًا بصريّة بعد التحديثات.
**التوصية:** اعتمد إبطالًا للذاكرة عند كل تغيير: إمّا أسماء ملفّات تحوي بصمة محتوى (hash)، أو إلحاق نسخة بالمرجع (`?v=2026-06-10`). بديل أبسط: تقليل `max-age` لملفّات JS/CSS وإزالة `immutable`.

### 🟡 منخفضة

#### L-1 — سياسة أمان المحتوى تسمح بالنصوص المضمّنة
**الملف:** `_headers` — `script-src 'self' 'unsafe-inline' 'unsafe-hashes' ...`
**الوصف:** السماح بـ`'unsafe-inline'` و`'unsafe-hashes'` يُضعِف الدفاع العميق ضدّ XSS، وهو ناتج عن استخدام معالجات أحداث مضمّنة (مثل `onclick="location.reload()"`) وسكربتات مضمّنة. الخطر العمليّ منخفض هنا لعدم وجود انعكاس لمدخلات المستخدم، لكنّه فجوة تصلّب.
**التوصية:** لاحقًا، انقل معالجات الأحداث إلى ملفّات خارجيّة واستبدل `'unsafe-inline'` بـnonce أو hashes للسكربتات، وأزل `'unsafe-hashes'`.

#### L-2 — مفتاح Web3Forms العلنيّ معرّض لإساءة استخدام النماذج
**الملفّات:** `contact.html`، `contact-en.html`، `index*.html`، `partials/footer*.html`، `site-shell.js`
**الوصف:** المفتاح علنيّ بالتصميم، لكن أيّ طرف يستطيع استخدامه لإرسال رسائل سبام إلى بريد الشركة. توجد مصيدة honeypot (جيّد)، لكنّها وحدها لا تكفي ضدّ الإساءة المُمنهَجة.
**التوصية:** فعّل في لوحة Web3Forms **تقييد النطاق (Domain Restriction)** على `elfaridaice.com`، وأضِف **hCaptcha** للنماذج.

#### L-3 — ترويسة `X-XSS-Protection` مهجورة
**الملف:** `_headers`
**الوصف:** `X-XSS-Protection: 1; mode=block` ترويسة قديمة تتجاهلها المتصفّحات الحديثة، وقد تسبّب سلوكًا غير مرغوب في حالات نادرة. غير ضارّة عمليًّا.
**التوصية:** يمكن إزالتها؛ فالحماية الفعليّة تأتي من CSP.

#### L-4 — توسّع في مصدر الصور بسياسة المحتوى
**الملف:** `_headers` — `img-src 'self' data: https: blob:`
**الوصف:** السماح بأيّ صورة من أيّ نطاق `https:` أوسع من اللازم.
**التوصية:** حصر `img-src` بالنطاقات الفعليّة المستخدمة (الذاتيّ + مزوّدو التحليلات/الخرائط) عند الإمكان.

### ⚪ معلوماتيّة (تجميليّة/تنظيميّة)

#### I-1 — `package.json` غير مكتمل
`main: index.js` يشير إلى ملفّ غير موجود، وحقول `author`/`description` فارغة، وسكربت `test` يخرج بخطأ افتراضيّ. لا أثر تشغيليّ، لكن يُستحسن تنظيفه.

#### I-2 — `.gitignore` يتجاهل `package-lock.json`
هذا غير معتاد؛ تجاهله يفقد تثبيت إصدارات التبعيّات. لا أثر أمنيّ، لكن يُنصَح بحفظه في المستودع.

#### I-3 — تأكيد وجود إعداد النشر في إدارة الإصدارات
لم يظهر ملفّ `wrangler.toml`/`wrangler.jsonc` ضمن المجلّد المفحوص؛ تأكّد أنّ إعداد نشر الـWorker محفوظ في المستودع لضمان قابليّة إعادة النشر.

---

## 5) تفصيل الجوانب الأمنيّة

- **حقن النصوص (XSS):** لا مسار استغلال مرصود. كل الإدراج الديناميكيّ يأتي من بيانات موثوقة مضمّنة (إعداد مجمَّد في `site-shell.js`) ومُهرَّبة، أو من قيم غير قابلة للتحكّم من المستخدم (أرقام مرجعيّة مولّدة، عدّادات). النماذج ترسل المدخلات في جسم طلب JSON فقط، لا إلى DOM.
- **الأسرار:** لا مفاتيح خوادم/رموز وصول سرّيّة في الكود. المفتاح الوحيد علنيّ بطبيعته.
- **النقل والترويسات:** HTTPS مفروض (HSTS + preload)، لا محتوى مختلط، حماية من التأطير والتنصّت على نوع المحتوى.
- **النماذج:** تحقّق من المدخلات + honeypot + مسار احتياطيّ (mailto) عند فشل الإرسال.

---

## 6) خاتمة وأولويّات الإصلاح

التقييم العامّ: **موقع آمن ومُتقَن هندسيًّا**، خالٍ من الثغرات الخطيرة. للوصول إلى الكمال، رتّب الإصلاحات هكذا:

1. **(متوسّطة) M-1:** إعادة ترتيب قواعد `_redirects` — إصلاح سريع بأثر مباشر على SEO.
2. **(متوسّطة) M-2:** اعتماد إبطال ذاكرة التخزين للأصول لضمان وصول التحديثات.
3. **(منخفضة) L-2:** تقييد نطاق Web3Forms + hCaptcha ضدّ السبام.
4. **(منخفضة) L-1:** تصليب CSP لاحقًا (إزالة `unsafe-inline`).
5. **(منخفضة/معلوماتيّة) L-3، L-4، I-1…I-3:** تحسينات تجميليّة وتنظيميّة عند توفّر الوقت.

> ملاحظة منهجيّة: هذا الفحص اعتمد على مراجعة الملفّات الأكثر حساسيّة وعمليّات بحث نمطيّة شاملة عبر المشروع، وهو دقيق ضمن هذا النطاق. لا يُغني عن اختبار اختراق ديناميكيّ حيّ (DAST) إن رغبت في طبقة تحقّق إضافيّة.
